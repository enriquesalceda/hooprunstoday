Score entry on Log Game. Two of them, side by side, 12–14px gap.

```jsx
<ScoreInput label="TEAM A (YOUR SQUAD)" value={a} onChange={setA} platform="web" />
```

Placeholder is `00`, never "0" or "Score". No stepper, no +/− buttons — this is typed.
