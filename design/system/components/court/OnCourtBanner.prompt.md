Appears inside the `CourtRow` the player is checked into — `CourtRow` renders it for you when `youAreHere` is set.

```jsx
<OnCourtBanner subdomain="prince-alfred-park.hoopruns.today" inset />
```

Keep the `▶` and the em-dash. Never restate it as "You're checked in!".
