Feed item under `THE BLACKTOP BULLETIN` — a 400px rail on web, a section under the directory on mobile.

```jsx
<BulletinRow handle="@jordan_buckets" when="4M AGO" court="PRINCE ALFRED"
  text="Squads forming on West Hoop. Need one to hold court."
  onHandleClick={() => openProfile('jb')} />
```

Body copy stays in the player's own voice — sentence case, street vocabulary, no moderation into brand voice.
