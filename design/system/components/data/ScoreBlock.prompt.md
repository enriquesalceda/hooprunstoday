Opens the STREET CRED tab. One per screen — a second hero number would flatten the hierarchy.

```jsx
<ScoreBlock value="875" rank="COURT LEGEND" platform="web" />
```

Follow it with a wrapping row of `Chip variant="outline"` badges and the `PEER-VOUCHED CAPABILITIES · N VOUCHES` footnote.
