The live indicator in app headers. Requires the `hr-pulse` keyframes from `tokens/motion.css`.

```jsx
<StatusDot /> <span>SYS_ACTIVE</span>
```

Never colored. Never larger than 8px. If something is not live, omit the dot rather than dimming it.
