Introduces a block of content. There are no visual headings in this system — this is the heading.

```jsx
<SectionLabel>COURT DIRECTORY // 3 TRACKED</SectionLabel>
```

Write it as telemetry, not as a title: ` // ` before counts, ` · ` between facts. Never sentence case.
